#include <stdio.h>
#include "main.h"
#include <math.h>

/**
 * add - Entry Point
 *
 * @a: first argument to be summed
 *
 * @b: second argument to be summed
 *
 * Return: Always 0 (Success).
 */

int add(int a, int b)
{
	int sum;

	sum = a + b;

	return (sum);
}
